import DesignSystem
import SwiftUI

struct RootView: View {
    var body: some View {
        Text("{{ name }}")
            .padding(DesignSystem.spacing)
            .accessibilityIdentifier("root-view")
    }
}

#Preview {
    RootView()
}
