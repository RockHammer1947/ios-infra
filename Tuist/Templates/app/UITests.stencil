import XCTest

@MainActor
final class {{ name }}UITests: XCTestCase {
    func testRootViewAppearsOnLaunch() {
        let app = XCUIApplication()
        app.launch()
        XCTAssertTrue(app.otherElements["root-view"].waitForExistence(timeout: 10))
    }
}
