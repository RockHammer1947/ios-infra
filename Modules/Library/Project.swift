import ProjectDescription
import ProjectDescriptionHelpers

// Reusable SwiftData persistence layer: user marks (highlights / notes /
// bookmarks) and per-chapter reading progress.
let project = Project.module(name: "Library")
