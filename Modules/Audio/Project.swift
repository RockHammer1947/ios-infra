import ProjectDescription
import ProjectDescriptionHelpers

// Reusable spoken-audio layer: system text-to-speech with a small, testable
// text-segmentation helper.
let project = Project.module(name: "Audio")
